{
  "name": "@tagma/cli",
  "version": "0.2.1",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/GoTagma/tagma-cli.git"
  },
  "type": "module",
  "bin": {
    "tagma": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "release": "bun scripts/release.ts",
    "release:publish": "bun scripts/release.ts --publish"
  },
  "engines": {
    "bun": ">=1.3"
  },
  "dependencies": {
    "@tagma/core": "latest",
    "@tagma/runtime-bun": "latest",
    "@tagma/sdk": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^6.0.2"
  }
}
